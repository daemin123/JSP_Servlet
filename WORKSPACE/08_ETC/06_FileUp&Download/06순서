//--------------------------------------------------------------
//파일업로드 테스트
//--------------------------------------------------------------
//-------------------------------
// [Controller] Frontcontroller
//-------------------------------
//@MultipartConfig 설정
//파일 업로드 정보
@MultipartConfig
(
		fileSizeThreshold=1024*1024*10,	// 10MB 업로드 파일이 10Mb이상이 될때 임시경로(temp)에 저장
		maxFileSize=1024*1024*50,		// 50MB  업로드 파일의 최대 크기 
		maxRequestSize=1024*1024*100	// 100MB request전체의 크기
)
 
//-------------------------------
//[table] tbl_upload_file
//-------------------------------
CREATE TABLE `bookdb`.`tbl_upload_file` (
  `id` INT NOT NULL auto_increment,
  `user` VARCHAR(45) NULL,
  `dir` VARCHAR(45) NULL,
  `filename` VARCHAR(45) NULL,
  PRIMARY KEY (`id`));

//-------------------------------
//[Dto] tbl_file_upload
//-------------------------------


//-------------------------------
// 
//-------------------------------
ㄴ

//--------------------------------------------------------------
//파일 다운로드 테스트
//--------------------------------------------------------------